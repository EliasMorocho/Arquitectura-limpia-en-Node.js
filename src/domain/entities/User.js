class User {
  constructor({ Email, Name, Role, State,Password }) {
    this.Email = Email;
    this.Name = Name;
    this.Role = Role;
    this.State = State;
    this.Password=Password;Ç
  }
}

module.exports = User;
